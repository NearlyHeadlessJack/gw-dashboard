[project]
name = "gw-dashboard"
version = "0.1.0"
description = "星网（GW）卫星星座数据仪表盘"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "curl-cffi>=0.15.0",
    "psycopg[binary]>=3.3.3",
    "pymysql>=1.1.2",
    "pyyaml>=6.0.3",
    "sqlalchemy>=2.0.49",
]

[dependency-groups]
dev = [
    "pytest>=8.0.0",
]
